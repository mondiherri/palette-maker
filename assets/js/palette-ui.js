window.PaletteUI = {
  init: () => {
    // Placeholder for palette-specific browser behaviors (sliders, previews, canvas rendering).
    // Future widgets can call PaletteUI.init() once the DOM is ready.
  },
};
